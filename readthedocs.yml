version: 2

sphinx:
    configuration: docs/conf.py

formats:
    - epub
    - htmlzip

python:
    version: "3.11"
    install:
        - method: pip
          path: .
        - requirements: docs/requirements.txt

submodules:
    include: all